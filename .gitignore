# Python
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so
*.pyd

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Python Virtual Environments
# If you create venv environments inside your project directory (Conda usually manages them globally or in user home)
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Secrets and API Keys - CRITICAL
# Ensure .env is listed. .env.example should be committed.
.env
*.env
*.env.*
!*.env.example
# If you ever store keys in other files by mistake (avoid this!)
# local_settings.py
# secrets.py

# IDE and Editor specific files
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/

# Other common editors
# .idea/ (for JetBrains IDEs like PyCharm)
# *.sublime-project
# *.sublime-workspace

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Local Data & Caches (Project Specific)
# Adjust these based on your actual local data storage strategy
#
# Compendium data downloaded from BustyBee (likely large)
data/compendium/
#
# Cached/processed transcripts if stored as intermediate files
data/transcripts_cache/
#
# Local ChromaDB storage folder (if created within the project root)
# The default is often a folder named 'chroma_data' or similar in the execution directory
chroma_data/
# Or if it uses SQLite files directly in a known path for other purposes:
# *.db
# *.sqlite3
#
# Whisper model cache (usually stored globally by Whisper, but if you configure a local path)
# .whisper_cache/

# Log files
*.log
logs/
*.out

# Temporary files
*.tmp
*.swp
*.bak
*~

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# Python test reports and coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# Gradio specific (if used for quick tests or remnants from v1)
# gradio_cached_examples/
# gradio_flagged/

# Temporary audio files (if explicitly saved in project, though tempfile usually uses system temp)
# temp_audio/*.mp3

# Conda environment files (if needed, though typically conda envs are managed outside the project dir)
# conda-meta/
# envs/
# .conda/

# Specific files from original structure if now generated/handled differently
# transcripts.txt # If it was a large raw dump and is now processed into ChromaDB
# evaluation_results... # If these are large binary files/logs vs. small text summaries